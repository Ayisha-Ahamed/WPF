<Window x:Class="CustomerManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomerManager"
        mc:Ignorable="d" Closed="OnClosed"
        Title="Customer Manager" SizeToContent="WidthAndHeight" FontSize="14" MinWidth="600" MaxWidth="800" MinHeight="500" MaxHeight="600">
   <Window.Resources>
      <Style TargetType="ListViewItem">
         <Setter Property="Padding" Value="10"/>
      </Style>
   </Window.Resources>
   <StackPanel Margin="20">
      <ScrollViewer MinHeight="350" MaxHeight="500" MinWidth="600" VerticalScrollBarVisibility="Auto">
         <ListView Name="CustomerList" SelectionMode="Single" MinWidth="350" ItemsSource="{Binding Customers}" 
                   HorizontalAlignment="Left">
            <ListView.Resources>
               <Style TargetType="GridViewColumnHeader">
                  <Setter Property="MinWidth" Value="150" />
                  <Setter Property="HorizontalAlignment" Value="Center" />
                  <Setter Property="HorizontalContentAlignment" Value="Center" />
                  <Setter Property="VerticalAlignment" Value="Stretch" />
               </Style>
               <Style TargetType="DataGrid">
                  <Setter Property="HorizontalAlignment" Value="Center" />
                  <Setter Property="VerticalAlignment" Value="Center" />
               </Style>
            </ListView.Resources>
            <ListView.View>
               <GridView>
                  <GridViewColumn Header="Id" Width ="50"  DisplayMemberBinding="{Binding Id}"/>
                  <GridViewColumn Header="First Name" DisplayMemberBinding="{Binding FirstName}"/>
                  <GridViewColumn Header="Last Name" DisplayMemberBinding="{Binding LastName}"/>
                  <GridViewColumn Header="Email" DisplayMemberBinding="{Binding Email}"/>
                  <GridViewColumn Header="Phone No" DisplayMemberBinding="{Binding PhoneNo}"/>
               </GridView>
            </ListView.View>
         </ListView>
      </ScrollViewer>
      <WrapPanel Grid.Row="3" Margin="5">
         <Button Command="{Binding ShowWindowCommand}" Content="Add" Click="OnClickedAdd"/>
         <Button Command="{Binding EditWindowCommand}" Content="Edit" Click="OnClickedEdit"/>
         <Button Name="Delete" Content="Delete" Click="OnClickedDelete"/>
      </WrapPanel>
   </StackPanel>
</Window>