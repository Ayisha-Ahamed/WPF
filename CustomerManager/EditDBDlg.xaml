<Window x:Class="CustomerManager.EditDBDlg"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomerManager"
        mc:Ignorable="d"
        Title="AddCustomer" MinHeight="400"  FontSize="14" MinWidth="400" SizeToContent="WidthAndHeight">
   <Window.Resources>
      <Style TargetType="Label">
         <Setter Property="HorizontalAlignment" Value="Right"/>
         <Setter Property="VerticalAlignment" Value="Center"/>
         <Setter Property="Margin" Value="5,0"/>
      </Style>
   </Window.Resources>
   <StackPanel Margin="20">
      <DockPanel LastChildFill="False">
         <StackPanel DockPanel.Dock="Right" Width="200" Height="auto">
            <StackPanel.Resources>
               <Style TargetType="TextBox">
                  <Setter Property="Margin" Value="5,10"/>
                  <Setter Property="Height" Value="30"/>
                  <Setter Property="VerticalContentAlignment" Value="Center"/>
                  <Setter Property="HorizontalContentAlignment" Value="Left"/>
                  <Style.Triggers>
                     <Trigger Property="Validation.HasError" Value="true">
                        <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, 
                           Path=(Validation.Errors)[0].ErrorContent}" />
                     </Trigger>
                  </Style.Triggers>
               </Style>
            </StackPanel.Resources>
            <TextBox Text="{Binding Customer.Id, Mode=OneWay}" IsEnabled="False"/>
            <TextBox Name="TxtFirstName" Text="{Binding Customer.FirstName,ValidatesOnDataErrors=True,UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Name="TxtLastName" Text="{Binding Customer.LastName,ValidatesOnDataErrors=True,UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Name="TxtMail" Text="{Binding Customer.Email,ValidatesOnDataErrors=True,UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Name="TxtPhNo" Text="{Binding Customer.PhoneNo,ValidatesOnDataErrors=True,UpdateSourceTrigger=PropertyChanged}"/>
         </StackPanel>
         <StackPanel DockPanel.Dock="Left" MinWidth="100" Margin="0,5">
            <StackPanel.Resources>
               <Style TargetType="Label">
                  <Setter Property="Margin" Value="5,10"/>
               </Style>
            </StackPanel.Resources>
            <Label Content="Id"/>
            <Label Content="First _Name" Target="TxtFirstName"/>
            <Label Content="_Last Name" Target="TxtLastName"/>
            <Label Content="E-_mail" Target="TxtMail"/>
            <Label Content="_Phone No" Target="TxtPhNo"/>
         </StackPanel>
         <TextBlock></TextBlock>
      </DockPanel>
      <WrapPanel HorizontalAlignment="Center">
         <Button Name="BtnExecute" Content="Add" Command="{Binding ExecuteOpCommand}"/>
         <Button Name="Cancel" Content="Cancel" Click="OnClickedCancel"/>
      </WrapPanel>
   </StackPanel>
</Window>
   
   